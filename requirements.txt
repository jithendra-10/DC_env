# Core
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# Data
pandas==2.2.2
numpy==1.26.4

# HTTP client (used by both baseline agents + server healthchecks)
httpx==0.27.0

# GPT-4o-mini baseline
openai==1.30.1

# Llama-3-70B baseline (HuggingFace Inference API)
# No extra package needed — uses httpx directly for HF API calls

# Gradio web UI
gradio==4.31.0

# Dev / testing
pytest==8.2.0
pytest-asyncio==0.23.6

# RL Training (Optional - for training_script.py)
# trl>=0.8.0
# transformers
# torch
# datasets
# accelerate
